# Frontend

React + Vite + TypeScript + Tailwind scaffold will be added in **v1.1 — Dashboard & Web UI**.

For now, use the API directly:

- Health check: `http://localhost:8000/health`
- API docs: `http://localhost:8000/docs`
